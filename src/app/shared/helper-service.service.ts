import { Injectable } from '@angular/core';

@Injectable()
export class HelperServiceService {

    constructor() { }

    // executarContagemRegressiva(tempoTotal: number, valorContagem: Number, acao) {

    //     this.valorContagemRegressiva = tempoTotal / (tempoTotal / 100);
    //     let idInterval = setInterval(() => {
    //         this.valorContagemRegressiva = this.valorContagemRegressiva.valueOf() - 1;
    //         if (this.valorContagemRegressiva <= 0) {
    //             acao();
    //             clearInterval(idInterval);
    //         }
    //     }, (tempoTotal / 100));

    // }

}
